**Reply on the existing ticket — do not open a new one.**

---

Following up on this. The block is unchanged after five days.

`hivecomb-win32-x64-msvc@0.1.0` has now been refused six times, from the same account
and the same granular access token, every time with:

```
npm error 403 403 Forbidden - PUT https://registry.npmjs.org/hivecomb-win32-x64-msvc
  - Package name triggered spam detection
```

| attempt | when (UTC) |
|---|---|
| 1 | 2026-09-05 15:57 |
| 2 | 2026-09-05 15:58 |
| 3 | 2026-09-05 17:13 |
| 4 | 2026-09-06 02:49 |
| 5 | 2026-09-09 04:31 |
| 6 | 2026-09-10 12:03 |

Spread over five days, so it is not rate limiting and it is not ageing out.

The four sibling packages remain published and untouched, from the same account, the
same token and the same CI run:

- `hivecomb-linux-x64-gnu` 0.1.0
- `hivecomb-linux-arm64-gnu` 0.1.0
- `hivecomb-darwin-x64` 0.1.0
- `hivecomb-darwin-arm64` 0.1.0

They follow the identical naming pattern, generated by napi-rs from the same root
package name. Only the Windows one is refused.

Because napi-rs stops at the first failed platform package, the root package
`hivecomb` has never been published either — so `npm i hivecomb` still returns nothing,
while four packages that nobody installs directly are live. The same release is
published and working on crates.io and PyPI as `hivecomb` 0.1.0.

Nothing has ever existed under the name `hivecomb-win32-x64-msvc`, so there is no
partial state to unpick — allowing the name is enough, and the next CI run publishes it
and the root package.

Is there anything further you need from me to clear it?

Source: https://github.com/flosolcher/hivecomb
