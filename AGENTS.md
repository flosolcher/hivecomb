# Agent instructions

See [CLAUDE.md](CLAUDE.md).

This file exists because some tools look for `AGENTS.md` and others for `CLAUDE.md`.
It is a pointer rather than a copy: two sets of instructions would drift, and the
one thing this project has learned the hard way is that a second copy of anything
is a defect waiting to happen.
